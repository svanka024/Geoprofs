@page
@using GeoProfs.Models;
@model GeoProfs.Pages.LeaveRequests.CreateModel

@{
    ViewData["Title"] = "Verlof Aanvragen";
}


<div class="row mb-auto justify-content-center">
    <div class="col-md-4">
        <div class="row no-gutters border mb-4">
            <div class="col p-4 mb-4 ">
                <h2 class="d-flex justify-content-center">
                    Leave request
                </h2>
                <hr>
                <div class="">
                    <p class ="d-flex justify-content-center">Leave type</p>
                    <div class="d-flex justify-content-betweend"></div>
                    
                    <label asp-for="LeaveRequest.Reason" class="control-label"></label>
                    <select asp-for="LeaveRequest.Reason.Id" asp-items="Model.ReasonTypes" class="form-control">
                    </select>
                    
                    </div>
                    <hr>
                    <div class="d-flex justify-content-center">
                        <form>
                            <label for="aInfo">Additional info:</label><br>
                            <textarea id="aInfo" rows="4" cols="50"></textarea>
                        </form>
                    </div>
                    <hr>
                    <div class="d-flex justify-content-center">
                        <form>
                            <label for="datemin">Start:</label><br>
                            <input type="date" id="datemin" name="datemin" min="2023-00-00"><br>
                        </form>
                        <form>
                            <label for="datemax">End:</label><br>
                            <input type="date" id="datemin" name="datemax" min="2023-00-00"><br>
                        </form>
                    </div>
                    <div class="d-flex justify-content-center">
                        <input type="submit" value="Submit">
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@*Please ignore unused code for dropdown, but fuck dropdowns! They are ugly af!

<select name="LeaveRequest" id="leaveRequest">
    <optgroup label="Leave reason">
    <option value="1">Sick</option>
    <option value="2">Vacation</option>
    <option value="3">Personal</option>
    </optgroup>
</select> *@

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
