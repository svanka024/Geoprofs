@page
@model GeoProfs.Pages.LeaveRequests.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>LeaveRequest</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="LeaveRequest.Id" />
            @*<div class="form-group">
                <label asp-for="LeaveRequest.Description" class="control-label"></label>
                <input asp-for="LeaveRequest.Description" class="form-control" />
                <span asp-validation-for="LeaveRequest.Description" class="text-danger"></span>
            </div>*@
            <div class="form-group">
                <label asp-for="LeaveRequest.Status" class="control-label"></label>
                <select asp-for="LeaveRequest.Status.Id" asp-items="Model.StatusItems" class="form-control">
                </select>
            </div><br />
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
